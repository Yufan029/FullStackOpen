{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "debug": "node --inspect index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../Part2/phonebook && npm run build && cp -r dist ../../Part3/phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m phonebook-fullstack-deploy && git push"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.18.1",
    "morgan": "^1.10.1"
  }
}
