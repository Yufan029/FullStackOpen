{
  "name": "blogs-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "lint": "eslint .",
    "fix": "eslint . --fix",
    "test": "cross-env NODE_ENV=test node --test",
    "start:test": "cross-env NODE_ENV=test node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../Part7/exercises7.9-7.21/bloglist-redux/bloglist-frontend && npm run build && cp -r dist ../../../../Part4/blogs-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m blog-fullstack-deploy && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "supertest": "^7.1.4"
  }
}
