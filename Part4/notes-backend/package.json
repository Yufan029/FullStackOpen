{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "seed": "cross-env NODE_ENV=test node mongo.js",
    "build:ui": "rm -rf dist && cd ../../Part2/Practice && npm run build && cp -r dist ../../Part4/notes-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint .",
    "fix": "eslint . --fix"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "supertest": "^7.1.4"
  }
}
